<section class="usp-strip" aria-label="{{ _('Store benefits') }}">
  <div class="container usp-grid">
    <div class="usp-item"><strong>{{ _('Quality parts') }}</strong><span>{{ _('Selected for reliability') }}</span></div>
    <div class="usp-item"><strong>{{ _('Fast delivery') }}</strong><span>{{ _('Delivery across Saudi Arabia') }}</span></div>
    <div class="usp-item"><strong>{{ _('Secure payment') }}</strong><span>{{ _('Trusted checkout experience') }}</span></div>
    <div class="usp-item"><strong>{{ _('Expert support') }}</strong><span>{{ _('Help finding the right part') }}</span></div>
  </div>
</section>
